# 校园AI问答助手：开发指南（详细实现步骤）

> 这是项目的详细开发指南；项目简介请看 README.md。

一句话：把自己的校园资料变成知识库，让大模型只根据这些资料回答问题，并标注来源。

## 它能讲出的"故事"

> 我发现同学们查选课、转专业等信息时，资料分散、答案不准确 → 我把学校公开资料整理成知识库 → 用向量检索 + 大模型实现问答助手 → 建立测试集逐个改进 → 准确率从 X% 提到 Y%。

这个"发现问题 → 动手解决 → 数据化改进"的故事，正是面试官想听的创新思维和自学能力。

## 目录结构

```
校园AI问答助手/
├── build_index.py      # 建库：切分资料 → 向量化 → 存 FAISS 索引
├── app.py              # 问答：检索 → 大模型生成答案 → Gradio 网页
├── data/               # 原始资料（.md/.txt），先放示例，再换成真实资料
├── index/              # 建库后自动生成（faiss.index + chunks.json）
├── requirements.txt    # 依赖清单
├── .env.example        # API Key 配置模板（复制成 .env 使用）
├── README.md           # 项目介绍（GitHub 首页）
├── 开发指南.md          # 本指南（详细实现步骤）
├── 测试集.md            # 18 题测试集与迭代记录
└── 演示脚本.md          # 演示视频脚本
```

## 第一步：准备环境（约 30 分钟）

1. 安装 Python 3.10 或更高版本（python.org，安装时勾选 Add Python to PATH）。
2. 打开 PowerShell，进入项目目录（`cd` 到"校园AI问答助手"文件夹）。
3. 创建并激活虚拟环境：

```powershell
python -m venv venv
venv\Scripts\Activate.ps1
```

4. 安装依赖（首次会下载 PyTorch，约 2GB，请耐心等待）：

```powershell
pip install -r requirements.txt
```

下载太慢可加国内镜像加速：

```powershell
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

5. 注册 DeepSeek 开放平台（platform.deepseek.com），充值少量金额（几块钱足够测试上百次），创建一个 API Key。
6. 把 `.env.example` 复制一份改名为 `.env`，填入你的 Key。

## 第二步：收集和整理资料（半天）

- 来源建议：学校官网、教务处官网、学院官网、选课通知、培养方案、常见问题页（只用公开信息，不要放内部文件）。
- 每个主题存成一个 `.md` 文件放进 `data/`，例如 `选课指南.md`、`转专业政策.md`、`培养方案.md`。
- 清洗规则：删掉导航、广告、重复段落，保留原文；先收集 5–10 份文件起步。
- 先用示例跑通：`data/` 里已有一份示例文件（内容是占位数据），全部跑通后再逐步替换成真实资料。

## 第三步：弄懂四个核心概念（先理解再动手）

- 切分（Chunking）：长文档切成小片段，问答时按片段检索。片段太小语义不全，太大检索不准。
- 向量化（Embedding）：把文字变成一串数字（向量），语义相近的文字向量也相近。
- 检索（Retrieval）：把你的问题也变成向量，在库里找最相似的几个片段（top-k）。
- 生成（Generation）：把"资料 + 问题"一起交给大模型，让它只根据资料回答。

这个方案叫 **RAG（检索增强生成）**，核心价值：不用重新训练模型，就能让模型回答你的专属资料，而且答案有依据、可溯源。

## 第四步：建库（运行第一条命令）

```powershell
python build_index.py
```

首次运行会自动下载中文嵌入模型（约 100MB）。完成后自动生成 `index/` 目录。

建库脚本只做四件事：读资料 → 切分 → 向量化 → 存索引。**请打开 build_index.py 逐行读懂，理解每一段在做什么，再继续下一步。** 面试时会从这里追问。

## 第五步：跑起问答界面

```powershell
python app.py
```

浏览器会自动打开（或手动访问 http://127.0.0.1:7860），输入问题测试。

app.py 的流程：加载资源 → 问题向量化 → 检索 top-5 → 拼提示词 → 调 DeepSeek → 返回答案 + 参考来源。

## 第六步：测试与迭代（这是拉开差距的关键）

1. 先写 10 个"测试问题"，每个问题都能在资料里找到答案。
2. 逐个问，把答错的记下来。
3. 三类常见错法及对策：
   - 资料里没有 → 补充资料；
   - 有但没检索到 → 把 top_k 调大（5→8），或把切分长度调小（300→200）；
   - 检索到了但答错 → 改提示词，强调"只根据资料回答""不确定就说不知道"。
4. 改完重测，记录准确率变化（比如 7/10 → 9/10）。**这个数字就是简历上的亮点。**

## 第七步：打磨成作品

- README 补上：项目简介、架构说明、效果截图、使用方法。
- 录 2 分钟演示视频：先说痛点 → 演示 3 个问答 → 展示参考来源。
- 写 1 篇技术博客（选一个踩坑经历：模型下载慢、切分断句、API 报错、答错改进过程）。
- 推到 GitHub（代码 + README + 演示视频链接）。

## 第八步：面试怎么讲

30 秒电梯演讲：

> 我发现校园信息分散、难查准。我收集了 XX 份公开资料，用向量检索 + 大模型搭了一个问答助手，回答带来源。我建了一个测试集，通过调切分和提示词，把准确率从 X% 提到了 Y%。

高频追问清单（提前写好答案）：

1. 为什么用 RAG 而不是直接问大模型？→ 大模型不知道你的资料，会编造；RAG 让答案有依据、可溯源。
2. 向量是什么？→ 文字的数学表示，语义相近的向量相近。
3. 切分为什么要带重叠？→ 防止关键句被从中间切断。
4. top_k 怎么定的？→ 试出来的：太大引入噪声，太小漏答案。
5. 答错了怎么办？→ 先判断是资料缺失、检索不到、还是生成错，分别解决。
6. 数据哪来的？→ 学校公开信息，自己整理清洗。
7. 哪些是你独立完成的？→ 如实回答，设计、建库、调优都是自己做的。
8. 用户多了怎么办？→ 加重排序、加缓存、部署到服务器。
9. 问题超出资料范围怎么办？→ 提示词要求它说"资料中没有相关信息"。
10. 为什么选 DeepSeek？→ 便宜、中文好、接口兼容 OpenAI；也可换通义、豆包等。

## 常见问题排查

- PyTorch 下载慢/失败 → 用上面的清华镜像重装依赖。
- 建库时报"没有数据" → data/ 目录里没有 .md/.txt 文件。
- 报错 `faiss::FileIOWriter ... could not open ... for writing` → 这是 faiss 在 Windows 上不支持中文路径。代码已改为用 Python 直接读写索引文件绕过此问题；如果仍有问题，把整个项目文件夹移动到纯英文路径（如 `C:\campus-ai`）再试。
- API 报 401 → .env 里 Key 没填对，改完要重启 app.py。
- 回答明显在编造 → 检查提示词，确认资料确实覆盖了问题。
- Gradio 打不开 → 手动访问 http://127.0.0.1:7860。

## 进阶方向（做完基础版后按需选择）

- 多轮对话记忆（把历史问答加入上下文）
- 重排序（用 bge-reranker 精排检索结果）
- 来源精确定位（返回原文件 + 段落号）
- 自动评测（写脚本批量测试准确率）
- 部署到云服务器，或接入微信 / 飞书机器人
